# Beginner – Challenge 01: Fade-in Animation

### 🎯 Goal

Animate a box so that it **fades in** from invisible (`opacity: 0`) to fully visible (`opacity: 1`) on page load using a basic GSAP tween.

---

## 📌 Challenge Requirements

- Use **GSAP.from()**, **GSAP.to()**, or **GSAP.fromTo()**
- Animation should start automatically on page load
- Duration must be between **1–3 seconds**
- Use the **default GSAP easing** (no custom ease)

---

## 🧠 What You Will Learn

- How to create a basic tween with GSAP
- How GSAP animates the **opacity** property
- How to trigger animations automatically on page load

---

## 📚 Resources Used

Recommended documentation before solving the challenge:

- GSAP Core – Tween Methods
  https://gsap.com/docs/v3/GSAP/gsap.from
  https://gsap.com/docs/v3/GSAP/gsap.fromTo/

- GSAP Getting Started Guide
  https://gsap.com/resources/get-started/

---

## 📝 What to Include in Your Solution

When you complete this challenge, write these in your solution README:

1. **What the challenge was about** – e.g., creating a fade-in effect for a box element
2. **Which GSAP methods you used and why** – `gsap.from()` or `gsap.fromTo()` for opacity animation
3. **Issues you faced and how you solved them** – e.g., DOM not ready, wrong selector
4. **Screenshots or GIF (optional)** – to show the animation in action

---

## 🚫 Important

This challenge focuses on the **basics only**.
Do **not** use:

- Timelines
- Custom easings
- Advanced GSAP utilities

Keep it simple.
